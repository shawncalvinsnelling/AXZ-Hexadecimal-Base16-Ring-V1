# Upload instructions

Upload every file and folder in this package to the GitHub repository root. If GitHub does not upload hidden files, manually recreate `.github/workflows/verify.yml` and `.gitignore` using the visible backup files.
