# Directional subtraction

Subtraction is directional. From a split `(left, right)`, the verifier inserts `left - right`, but does not also insert `right - left`.

This preserves the exact syntax of ordered binary expression trees.
